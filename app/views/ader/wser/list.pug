extends ../aalayout/layout

block content
	.container.mt-3
		table.table.table-hover.table-bordered
			thead
				tr
					th 账户
					th 编号
					th 角色
					th 公司
			tbody
				each object in objects
					-a;
					tr(class="object-id-"+object._id)
						td: a(href=thisAct+"/"+object._id) #{object.code}
						td #{object.cd}
						td #{Conf.wsRole[object.role]}
						td
							if object.group
								-group = object.group;
								span #{group.code}